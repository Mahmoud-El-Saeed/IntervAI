export function SettingsPage() {
  return (
    <section className="glass-card rounded-lg p-6">
      <p className="font-code text-xs uppercase tracking-[0.1em] text-primary">Settings</p>
      <h2 className="mt-3 font-headline text-3xl font-bold">Platform Preferences</h2>
      <p className="mt-2 text-sm text-on-surface-variant">
        Settings controls can be expanded here as backend profile endpoints become available.
      </p>
    </section>
  );
}
